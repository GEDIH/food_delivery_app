# pubspec.yaml (Dependencies snippet)

dependencies:
  flutter:
    sdk: flutter
  # State Management (modern practice)
  flutter_bloc: ^8.1.3  
  equatable: ^2.0.5     # Used for value equality in PaymentState
  
  # ... (other existing dependencies: firebase, google_maps, etc.)
